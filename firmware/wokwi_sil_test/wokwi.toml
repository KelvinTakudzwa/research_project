[wokwi]
version = 1

elf      = ".pio/build/wokwi_sil_test/firmware.elf"
firmware = ".pio/build/wokwi_sil_test/firmware.bin"

# Routes ESP32 UART0 to a local TCP port so PlatformIO's
# serial monitor can connect and display Serial.print() output.
